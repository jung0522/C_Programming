/*
#include <stdio.h>
// 결과를 출력하기 위해 for문을 사용하고자 한다.//
int main(void)
{
	for (int i = 2; i != 10; i++)  // 2와 곱해지며, 1씩 증가하는 정수형 변수 i를 선언해 9단까지 출력하도록 하는 조건식과 증감식을 설정해준다. //
	{
		printf("%d * %d = %d\n", 2, i, 2 * i); // 2, 곱해지는 수, 그리고 그 결과값을 서식문자로 지정해준다. //
	}
	return 0;
}
*/

