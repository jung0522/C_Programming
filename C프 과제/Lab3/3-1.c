/*
#include <stdio.h>

int main(void)
{
	while (1) {
		int num = 0;
		printf("10진수를 입력하세요: "); // 10진수를 입력 받는다 //
		scanf("%d", &num);
		
		printf("입력 받은 수의 8진수로는 %o 16진수로는 %X입니다.\n", num, num); //  8진수로 나타내는 서식 문자 %o와 16진수로 나타내는 서식문자 %X를
		                                                                     //  통해 입력받은 10진수의 8진수 형태와 16진수 형태를 출력한다. 
	}
	return 0;
}\*/

