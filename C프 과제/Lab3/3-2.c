/*
#include <stdio.h>

int main(void) {
	int i;
	double sum = 0.0; // float에서 double로 //

	for (i = 0; i < 1000; i++)
		sum = sum + 0.1;

	printf("0.1을 1000번 더한 결과: %lf\n", sum); // 자료형을 float형에서 double로 바꿔졌기 때문에 표현할 수 있는 범위가 넓어졌다. //
	return 0;
}
*/