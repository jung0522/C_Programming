/*
#include <stdio.h>

int main()
{
	double i = 365;
	int j = 3.1415;
	int k = 129;
	char ch = k;

	printf("%f\n", i); //자료형이 double인 i(365)를 %f로 서식문자를 지정해주었기 때문에 365.000000이 출력된다. //
	printf("%d\n", j); //자료형이 int인 j(3.1415)를 %d로 서식문자를 지정해주었기 때문에 소수점이 생략되고 정수 3이 출력된다. //
	printf("%d\n", k); //자료형이 int인 k(129)를 %d로 서식문자를 지정해주었기 때문에 129가 출력된다. //
	printf("%d\n", ch); //자료형이 char인 ch(k(129))를 %d로 서식문자를 지정해주었기 때문에 char(1byte)는 
	                    // -128 ~ 127까지만 표현 가능하므로 -127이 출력된다. //

	return 0;
}
*/