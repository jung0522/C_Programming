/*
#include <stdio.h>
int main()
{
	double f1;
	double f2;
	printf("1st double: "); 
	scanf("%lf", &f1); // %lf를 서식 문자로 하여 double형 f1입력받음 //

	printf("2st double: ");
	scanf("%lf", &f2); // %lf를 서식 문자로 하여 double형 f2입력받음 //

	printf("%lf + %lf = %lf\n", f1, f2, f1 + f2); // %lf를 서식문자로 하여 f1, f2, f1 + f2를 출력한다 //
	printf("%lf - %lf = %lf\n", f1, f2, f1 - f2); // %lf를 서식문자로 하여 f1, f2, f1 - f2를 출력한다 //
	printf("%lf * %lf = %lf\n", f1, f2, f1 * f2); // %lf를 서식문자로 하여 f1, f2, f1 * f2를 출력한다 //
	printf("%lf / %lf = %lf\n", f1, f2, f1 / f2); // %lf를 서식문자로 하여 f1, f2, f1 / f2를 출력한다 //

	return 0;
}
*/