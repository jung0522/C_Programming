/*
#include <stdio.h>
int main(void)
{
	int count = 1;
	
	// 대문자와 소문자가 번갈아 나오는 규칙을 반복문을 통해 이용한다
	for(int ch = 'a'; ch < 'z'; )
	{
		for(int i = 0; i < count; i++)
		{
			if(ch % 2 != 0)
				{
			    printf("%c", ch);
				ch++;
			}
			else
				{
                printf("%c", ch-32);
				ch++;
                if(ch == 'z'+1)
					break;
			}
			
		}
		printf("\n");
        count++;
		
	}
		return 0;
}
*/


  
