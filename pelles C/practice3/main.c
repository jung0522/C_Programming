
/*
#include <stdio.h>
#include <string.h>


int main()
{
  char perID[7];
  char name[7];
	

	while(1)
		{
 
		fputs("주민번호 앞 6자리 입력: ", stdout);
		fgets(perID, sizeof(perID), stdin); // 사이즈를 오버하면 그전까자 인식

        if( strcmp(perID, "x\n") == 0)
			break;

		while(getchar() != '\n')
			{
		}

		printf("주민번호: %s\n", perID);

		

 
		fputs("이름 입력: ", stdout);
		fgets(name, sizeof(name), stdin); // 사이즈를 오버하면 그전까자 인식

		while(getchar() != '\n')
			{
		}

		printf("이름: %s\n", name);
       }
	
	return 0;
}
*/

