/*
#include <stdio.h>


void ShowAddResult(int , double );  // 함수 선언 변수명 생략 가능 //


void HowToUseThisProg() {
	("두 개의 정수를 입력하시면 덧셈 결과가 출력됩니다.\n");
	("두 개의 정수를 입력하세요. \n");
}

int ReadNum(void) {
	int num;
	scanf_s("%d", &num);
	return num;
}

int Add(int num1, int num2) {
;
	return num1 + num2;
}

void ShowAddResult(int result, double avg) {
	printf("덧셈결과 출력:%d\n", result);
	printf("평균 출력:%lf\n", avg);
}

double Avg(int num1, int num2) {
;
	return num1 + num2 / 2.0;  //
}

int main(void) {
	int result, num1, num2;
	double avg;

	HowToUseThisProg();
	num1 = ReadNum();
	num2 = ReadNum();
	result = Add(num1, num2);

	double Avg(int, int);   // main 함수 안에 넣어도 됨 //

	
	avg = Avg(num1, num2);
	ShowAddResult(result, avg);
	return 0;
}
*/
