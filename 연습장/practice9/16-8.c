#include<stdio.h>
#include<math.h>
/*
struct point{
	int xpos;
	int ypos;
};


int main(void) {
	struct point pos1, pos2;
	double distance1;
	double area;

	printf("point1 pos: ");
	scanf("%d %d", &pos1.xpos, &pos1.ypos);
	printf("point2 pos: ");
	scanf("%d %d", &pos2.xpos, &pos2.ypos);

	area = (abs(pos1.xpos - pos2.xpos) * abs(pos1.ypos - pos2.ypos));
	printf("area is: %lf", area);
	


	return 0;
}
*/